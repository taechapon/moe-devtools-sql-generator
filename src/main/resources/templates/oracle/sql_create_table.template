--------------------------------------------------------
-- DDL for Table ${tableName}
--------------------------------------------------------
CREATE TABLE "${tableName}" (
${columnList}
${primaryKeyList}
);
${commentList}
